# kepco-tes-analysis 개정 이력

> **v17(2026-07-20)**: **산출 엑셀을 한 개로 통합.** 빌더가 별도 파일을 뱉던 구조를 바꿔
> `build_load_calc.py`·`build_ops_plan.py`에 `--into` 옵션을 추가했다. 기존 워크북을 열어
> 자기 시트를 갈아끼우고(`replace_sheet`) 표준 순서로 정렬한 뒤(`order_sheets`) 저장하므로,
> 실행 순서·횟수와 무관하게 항상 같은 12시트 파일이 나온다(멱등). `--into` 없이 돌리면
> 종전대로 단독 파일이 나와 단일 문서 검증 용도는 유지된다.
> 함께 고친 것 —
> ① `_normalize_zip`이 임시파일 `os.replace`로 저장하다 Windows 드라이브 마운트에서
>    WinError 5로 실패하던 문제(재저장 경로에서만 발현)를 메모리 경유 덮어쓰기로 수정.
> ② `--into` 모드에서 시트3의 `[5] 확인사항` 블록을 생략(시트12가 전담 — phase2 규정이었으나
>    빌더가 지키지 않고 있었다).
> ③ ops JSON에 `"미수록": true` 지원 — 난방표가 없는 문서에서 표를 지어내지 않으면서
>    12시트 구성을 유지한다.
> ④ 교차검증 카탈로그에 **K6(건물부하계산서 미첨부)·K7(냉방 최대부하 대조 불가)** 추가.
>    부하계산서가 `무`인 문서에서 체크리스트 행7·행10이 ⚠️가 되는데 §5에 대응 형식이 없어
>    임기응변 문구를 쓰게 되던 구멍을 막았다. **행↔코드 매핑표(7→K6, 9→K1, 10→K7, 27→P2)**를
>    명시하고, 표에 없는 행은 ①에 넣지 말고 **카탈로그 코드를 먼저 추가**하도록 규칙화했다.
>    K6의 `{미첨부 사유}`는 4개 어휘로 고정(`간지만 수록`·`결과값만 수록`·`설계서로 대체 수록`·`자료없음`).
> ⑤ **숫자 표시형식 규칙 추가** — 모든 숫자·수식 셀에 천단위 쉼표 `#,##0.#`, 비율·%·LPM/RT는
>    고정 예외 목록(`0.0`/`0.000`/`0.00`). 값이 아니라 표시형식만 바꾸므로 검증 수식에 영향 없다.
> ⑥ **열원기기 용량 판정에 부분축열 반영** — `열원기기` 비교 블록을 행35~44로 넓혀
>    B41(축열조 피크 방열 담당, RT 값)·B42(`=B37+B41`)를 넣고 판정을 `B43=IF(B42>=B39,…)`로 바꿨다.
>    종전 `B41=IF(B36>=B38,…)`는 열원 단독 비교라, 부분축열 설계에서 체크리스트 행22(✅)와
>    시트8(❌ 용량 부족)이 엇갈렸다. decision_rules §1 행22의 판정식과 이제 같은 식을 계산한다.
>    판정 행이 B41·B42 → B43·B44로 이동(외부 참조는 B37·B40뿐이라 영향 없음).
> ⑦ **열원기기 용량 적정성을 운전모드별로 재설계** — 종전 '총용량 vs 최대냉방부하' 단일 비교를
>    폐기하고 `제빙↔심야(22:00~08:00) 축열운전 최대`·`냉방↔주간(08:00~22:00) 직접담당 최대`·
>    `난방↔난방운전 최대`로 나눠 비교한다(행35~50). 판정은 **부족 <100% / 정상 100~130% /
>    과다 ≥120%**(과다 기준 150%→120%). 카탈로그 **H3(용량 부족)·H4(과다 산정)** 추가,
>    체크리스트 행22·23 문구·참조 갱신. A빌딩에서 **제빙 용량비 95.9%(부족)** 이 새로 잡혔다.
> ⑧ **`업체확인필요사항`을 맨 앞 시트로 옮기고 양식만 생성** — 사용자가 직접 작성하는 시트이므로
>    헤더 4열(`번호`/`구분(시트/단계)`/`확인필요항목 및 내용`/`업체 답변`)만 두고 행4 이하는 비운다.
>    분석 발견사항은 `체크리스트`(행7~27 판정 / 행28~ 카탈로그)가 전담한다.
> ⑨ **`.#` 숫자 서식 폐기** — 엑셀이 `#,##0.#`에서 소수부가 없으면 `900.`처럼 소수점만 남겨
>    표시하는 문제. 정수 `#,##0` / 소수·수식 `#,##0.0` / 용량비 `0.0%`로 나눴다.
> ⑩ **축열조 용량 계산자료 유무 확인 절차 신설**(축열조 행62~66). 종전에는 스킬이 치수·IPF로
>    축열능력을 재계산해 대조하는 '검증'만 있어, 계획서가 산출근거를 냈는지 여부와 무관하게
>    ✅가 나왔다(결과값만 적어 내도 통과). 건물부하계산서 시트3 `[1]`과 같은 취지로 수록 여부를
>    따로 기록한다. `{수록 형태}`는 4개 어휘 고정(`용량 산출내역 수록`·`대수 산정만 수록`·
>    `결과값만 수록`·`미수록`), 카탈로그 **T3(축열조 용량 계산자료 미비)** 추가, 체크리스트 행25
>    판정에 계산자료 `무` 분기 추가.
> ⑪ **설비 시트를 설비 목록과 1:1로 확장** — `냉각탑`·`링블로워` 신설(시트 12→14).
>    `축냉설비설치계획`에 목록만 있고 검증 시트가 없던 기기를 각자 시트에서 검증한다.
>    냉각탑: 1 CRT=3,900 kcal/h 환산 검증 + **냉각탑 용량 vs 냉동기 응축 방열량**(냉방능력+소비전력)
>    + 입력(kW) 대조(H2). 링블로워: 축열조 교반 필요공기량 대조 + **풍량 표기 대조**(X2를 기계 판정).
>    설비 목록에 없는 기기의 시트는 만들지 않는다(공랭식이면 냉각탑 없음).
> ⑫ **수록 위치 표기 신설** — 설비·현황 시트 `B1`에 `수록: {파일명} p.{n} ({절 제목})`,
>    체크리스트 **F열을 '비고'에서 '근거 위치'로 변경**해 전 행에 파일명·페이지를 적는다.
>    검토자가 원문을 바로 펼칠 수 있게 하는 목적.
> ⑬ **각 시트 확인사항을 체크리스트로 통합** — 카탈로그 다음에 `시트확인` 블록을 두어
>    시트3 `[5]`·시트6·7 `[4]`·시트9 산출근거 판정 등을 한곳에 모은다.
>    `업체확인필요사항`이 빈 양식이므로 분석 결과는 전부 체크리스트에 모인다.
> ⑭ **kcal·kcal/h 셀은 항상 소수점 없이**(`#,##0`). A열 항목명 또는 C열 단위에 `kcal`이 있으면 적용.
> ⑮ **출처(수록 위치) 열 신설 + `상호검증` 시트 추가**(시트 14→15). 설치계획서는 같은 수치를
>    본문 요약표·규격표·설계서·데이터시트에 중복 기재하므로, 값마다 출처를 남기고 여러 곳에
>    있으면 전부 나열한다. 중복 수치 23항목을 `상호검증` 시트에 모아 `MAX-MIN<=허용오차` 수식으로
>    대조한다(A빌딩: 일치 12 / 불일치 10 / 단일출처 1 — 불일치는 전부 기존 카탈로그 코드에 대응).
> ⑯ **축열조 계산자료를 산출 입력값별로 세분화**(행62~72). 축열능력은 크기→매체 종류·체적→
>    운영온도→제빙율→잠열·현열 계산 순으로 도출되므로 ①~⑥ 항목별 수록 여부를 각각 찍는다.
>    A빌딩는 ①~⑤ 입력값은 모두 있으나 **⑥ 잠열·현열 계산식만 없어** `대수 산정만 수록`.
> ⑰ **설비 시트명에서 `규격` 제거** — `열원기기규격`→`열원기기` 등 6개. A1 제목도 동일.
>    교차참조 3곳(`축열조'!B56`·`열원기기'!C7`·`열원기기'!C21)과 카탈로그 구분값 일괄 갱신.
> ⑱ **`감소전력계산` 시트 신설**(시트 15→16). 수요관리업무처리지침 제2장 1.나(p.81~83) 산식으로
>    감소전력을 재산정해 기재값과 대조한다. (가) 축열조 저장가능열량×95% · (나) 열원기기
>    축열열량×90% · (다) 고객 제시 세 값을 모두 계산해 `MIN`을 채택하고 근거를 수식으로 표시.
>    카탈로그 **E3(감소전력 산정 불일치)** 추가. A빌딩: 기재 381 kW vs 지침 산식 351 kW
>    ((나) 채택) — 계획서가 (다) 고객제시값만 적용해 (나)를 누락한 사례.
> ⑲ **설비 목록 누락 검증 블록 신설**(시트5 행53~). `축냉설비설치계획` 1) 설비 목록과 뒤쪽 설비
>    시트를 양방향 대조한다. D열이 대응 시트의 제작사·모델을 **수식으로 끌어오므로** 시트가 없으면
>    판정이 `❌ 시트 누락`이 된다(존재 여부만 보지 않는다). 역방향(시트는 있는데 목록에 없음)도
>    확인하며, 축열조는 `2) 냉온열축열조` 블록 기재가 정상이므로 오판하지 않도록 명시.
>    카탈로그 **E4(설비 목록 vs 설비 시트 불일치)** 추가. A빌딩: 8건 전부 대응 ✅ 누락 없음.
> ㉑ **버그 수정 — 표시용 문자열이 수식으로 저장되던 문제.** openpyxl은 `=`로 시작하는
>    문자열을 수식으로 저장하므로, 산식 설명(`= 냉방에 이용가능한…`)·출처 표기(`=C7 (본 시트)`)처럼
>    읽으라고 쓴 문자열이 엑셀에서 **"제거된 레코드: 수식" 경고와 함께 사라졌다**
>    (sheet8.xml = 감소전력계산). 앞에 설명어를 붙여 텍스트로 만들고, 전역 표기 규칙에
>    **표시용 문자열 `=` 시작 금지**를 명문화. 필수검증 9-1(수식 셀 유효성) 추가 —
>    따옴표 밖 한글·`= ` 시작·출처 열의 `=` 시작을 잡는다(한글 시트명 참조는 작은따옴표
>    구간 제외로 오탐 방지). 전체 262개 수식 셀 재검사 이상 0건.
> ㉒ **상호검증 허용오차를 상대오차로 고정**(decision_rules §5.5 신설). 연면적·심야전력
>    설비용량 **0.1%**, 유량·열량 **1%**, 그 외 **완전일치**. 시트에 `허용기준` 문구 열과
>    `허용오차(수식)` 열(`=C{r}*0.001`)을 두어 값이 바뀌어도 자동 반영된다. 이에 따라 카탈로그
>    **K2(연면적 0.002%)·X1(열교환기 유량 0.44%)·E1(입력합계 0.045%)이 허용 범위 내로 판정되어 제외**되고,
>    **T2(축열조 내부체적 vs 총유체량)는 애초에 다른 값이라 폐기**했다. 카탈로그 22 → 18건,
>    상호검증 불일치 10 → 6건.
> ㉓ **펌프 용도 구분 5종 신설**(decision_rules §2). 열원·축열·방열·축방열(겸용)·부하(2차측)로
>    나누고 용도별 담당 RT 분모를 각각 규정. 겸용 펌프는 방열 기준(주간 피크 담당)으로 잡는다.
>    시트 `s+1` 행 표기를 `{설비명} ({용도 구분} / {순환유체})`로 확장.
> ㉔ **`scripts/verify_output.py` 승격** — 작업 중 Temp에만 있던 산출물 검사기를 스킬로 올렸다
>    (v16.1에서 scan_pdf·verify_repro를 승격한 것과 같은 취지). SKILL.md의 산문 규정이던
>    '완성 후 필수 검증'을 **실행 가능한 검사 16종**으로 자동화하고, 실패 시 종료코드 1로
>    빌드 파이프라인을 막는다. **시트 목록·열 위치를 하드코딩하지 않는다** — 현장마다 설비
>    구성이 다르므로 필수 시트만 확인하고(선택: 실별부하집계·냉각탑·링블로워), 출처 열은
>    헤더 문구로 자동 탐지하며, 펌프 블록은 `[펌프 ` 라벨로 개수와 무관하게 검사한다.
>    검사 13~16(`.#` 서식 / kcal 소수점 / 잘못된 수식 / 출처 열 수식 저장)은 엑셀이 파일을
>    손상 처리하거나 값을 조용히 바꾸는 유형으로, 눈으로는 놓치기 쉽다.
>    승격 과정에서 검사기가 실제 문제 2건을 잡았다(상호검증 요약행이 데이터 행으로 계수됨,
>    감소전력계산 B1 접두어 불일치 → `근거:` 허용으로 정정).
> 판독값 JSON은 종전대로 **작업 폴더에 유지**한다(R8의 원천이므로 삭제 금지).
> 검증: A빌딩 3단계 파이프라인 2회 독립 실행 → 12시트·순서·sha256 동일, 필수검증 10/10.
> 단독 모드 회귀 2건 v16 대장 sha256과 바이트 동일. 시트12 20행 → 22행(카탈로그 16→18).

> **v16.1(2026-07-20)**: 작업 중 쓰던 도구 3종을 임시 폴더에서 스킬로 승격.
> `build_survey.py`(여러 현장 정리표), `scan_pdf.py`(PDF 키워드 1차 스캔),
> `verify_repro.py`(재현성 검증). 정리표에 하드코딩돼 있던 26개 현장 데이터는
> 작업 폴더의 `survey_*.json`으로 분리했다(R8 — 도구는 스킬, 판독값은 작업 폴더).
> SKILL.md에 일괄 검토 워크플로 추가. 검증: 25건(현장 23 + 정리표 2) 셀·sha256 전건 동일.

> **v16(2026-07-20)**: 문서 구조를 **시트 하나 = 문서 하나**로 재편. `output_template.md`(289줄)를
> 해체해 시트1·2→phase1, 시트4→phase3, 시트5·8~11→phase5로 옮기고, 전역 표기 규칙·A1 제목표·
> 시트12 기재 규칙은 SKILL.md로 흡수했다. 셀맵을 두 문서가 나눠 소유하던 구조가 v15.2까지 발견된
> 불일치(값/수식 충돌, 끊긴 교차참조, 이중 항목표)의 공통 원인이었다.
> 개정 이력을 이 파일로 분리하고, SKILL.md에 파일 구성표를 추가했다. 배정이 없던 시트2·5·11도
> 담당 Phase를 갖게 됐다. .hwp 서식은 `assets/forms/`로 이동.
> 검증: 교차참조 23건 전수 해석 확인, 산출물 23개 셀·sha256 동일 유지.

> **v15.2(2026-07-20)**: Phase 5(열원기기·축열조·펌프) 점검. 열원기기 비교 블록 B38이 문서마다
> 값/수식으로 갈리던 것을 교차참조 수식으로 통일(R5), 축열조 **합계·비교검증 블록을 행55~60으로
> 고정**(수축열식일 때 체크리스트 행25의 `B56` 참조가 깨지던 문제), 펌프 항목표 이중 규격 제거,
> 물성치 '조회값' 문구를 고정값 참조로 정정.
>
> **v15.1(2026-07-20)**: v14 개정 때 사라졌던 **심야부하 검증을 V12~V14로 복원**(심야 부하 합계 /
> 심야 최대 ≤ 주간 최대×60% / 심야 부하량 ≤ 일일×40% — KEPCO 심사항목). 심야 행은 시각 라벨에서
> 자동 판별하고(구간 표기는 앞 숫자=시작시, 단일 표기는 종료시) 결과를 표준출력에 보고하며,
> JSON `심야_시각`으로 덮어쓸 수 있다. 체크리스트 행15·16·17 교차참조를 새 셀(C50~C52)로 연결.
> 문서 전반의 v12 잔재 정리: 시트6·7 템플릿의 `×3.517` 환산 규칙 폐기, decision_rules §4 교체,
> 현장 전용 예시값 일반화.
>
> **v15 변경(2026-07-20)**: **case-pack 폐지**. 문서별 셀맵을 통째로 임베드하던 결정론 빌더
> (`assets/cases/build_case_*.py`)와 고정값 대장(`references/case_*_frozen.md`)을 제거하고,
> **R8(판독값 JSON + 범용 빌더)로 일원화**한다. case-pack은 값과 셀맵이 한 파일에 엉켜 있어
> 템플릿을 개정하면 통째로 낡는 반면(실제로 v13 개정 후 구 형식 산출), JSON 방식은 값만 분리돼
> 빌더를 고치면 전 현장이 자동으로 새 규격을 따르고 바이트 동일도 유지된다.
> A빌딩도 `loadcalc_A빌딩.json`·`ops_A빌딩.json`으로 이관(부하계산서 유무=`무`).
>
> **v14.1(2026-07-20)**: 빌더 산출물의 **바이트 재현성** 확보. `scripts/_xlsx_det.py` 추가 —
> openpyxl이 저장 시각으로 덮어쓰는 `docProps/core.xml`의 dcterms:created/modified와 zip 멤버 시각을
> 고정값으로 정규화한다. 두 빌더 모두 `save_deterministic()`으로 저장하고 sha256을 출력한다.
> 22개 산출물 3회 독립 실행 검증: 셀 22/22, **sha256 22/22 동일**.
>
> **v14(2026-07-20)**: `시간대별 운전계획` 검증 재정의. **① 수록 위치 확인 → ② 시간대별 부하량
> → ③ 열원기기 운전계획 → ④ 열원기기→축열조 축열량 → ⑤ 축열조 누적** 순으로 계산·검증한다.
> **단위 무변환 원칙**(RT는 RT, RTh는 RTh, kW는 kW — 환산 금지)과 **누계 기준시각 = 축열 시작 직전**
> 규칙을 고정. 24시간 표를 축열 시작 시각이 첫 행이 되도록 회전시켜 누계를 다시 쌓는다.
> 빌더 `scripts/build_ops_plan.py`(스키마 `assets/opsplan_schema.example.json`) 추가 — V1~V14 검증
> (합계 대조·수지 균형·최종 누계 0·설계용량 초과·축열률 40%·부하 정합·기재누계 대조) 자동 수행.
>
> **v13(2026-07-19~20)**: `건물부하계산서` 시트를 **단순화 + 검증 강화**.
> **(1) 유무 → (2) 공간별 면적·부하 전사 → (3) 합산·최대부하율** 로 재정의하고,
> 건물명·계산방법·작성주체·일부하량(RTh) 항목을 제거. **(1)이 `무`이면 이후 계산을 하지 않는다**(억지 채움 금지).
> **[2] 면적 계산검증 블록 신설**(전사 누락·냉난방 면적차·설치계획서 대조·평 환산·연면적 비율)과
> 공간별 표의 **`면적 × 단위부하` 행별 재계산 검증**(I·J·K열) 추가.
> 계산서에 실별 상세표가 있으면 **`실별부하집계` 시트에 전량 전사 후 합계 대조로 판독 정확도를 증명**한다.
> 교차참조 앵커는 B11(면적)/B22(냉방RT)/B25(난방RT).
> 셀맵을 코드로 고정한 **범용 빌더 `scripts/build_load_calc.py`** 추가(입력 스키마:
> `assets/loadcalc_schema.example.json`) — 문서별 판독값은 작업 폴더 JSON에 두고 스킬에는 넣지 않는다.
>
> **v12(2026-07-15)**: 스캔 이미지 PDF 재현성 확보. A빌딩 문서를 3회 독립실행 →
> 셀단위 비교(53셀 발산, 96.87%) → 발산 유형별 고정 규칙화 → **고정 데이터셋 임베드 결정론
> 빌더(case-pack)** 커밋. 재실행 5회 sha256 동일 검증(1,695셀 100% 일치). **→ v15에서 폐지, R8로 대체.**
