{{HOSTNAME}}
# 이 루틴을 실제로 돌리는 PC 이름. 러너가 첫 줄만 읽는다(`hostname` 출력과 대조).
#
# 왜 필요한가 — 루틴 폴더를 클라우드 동기화 폴더(Dropbox·OneDrive 등)에 두고 두 PC 에
# 스케줄을 걸면, 같은 시각에 함께 돌아 같은 폴더에 함께 쓴다. 로그·첨부에 충돌 사본이
# 생기고 아카이브가 한 회차를 두 번 소비하며, 보고 이력이 반쪽만 반영되면 다음 주에
# 이미 본 공고를 신규로 다시 보고하게 된다.
#
# 동기화 폴더는 잠금이 아니므로 flock 으로는 PC 간 중복을 막을 수 없다. 그래서 호스트명이다.
# 소유 PC 를 옮기려면: 이 파일 첫 줄을 새 PC 이름으로 바꾸고,
# 이전 PC 의 스케줄러에서 작업을 지운다(원격으로는 지울 수 없다).
