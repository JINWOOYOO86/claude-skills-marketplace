## STATUS: OK
양식출처: 기본양식(폴백) — `default_proposal_form.pdf` (규칙의 유일한 출처)

# 기본 양식 작성 가이드라인

> 이 파일은 `template-extraction` 스킬이 **기본 양식으로 폴백할 때 `01_template_rules.md` 로 그대로 복사해 쓰는** 사전 추출본이자, 사람이 읽는 작성 가이드다.
> 공고 첨부 서식이 있으면 이 파일을 쓰지 말고 그 서식에서 새로 추출한다.
>
> **규칙의 출처는 PDF 한 곳이다.** `default_proposal_form.md` 는 그 PDF의 전사본이고, 아래 규칙은 전부 PDF 안내문·목차·[표 A]·[표 B]에서 나왔다. HWPX 빈 양식 파일은 두지 않는다 — 조립은 도구 기본 템플릿으로 하고 PDF 규칙을 헤더 패치로 적용한다(§3).

## 1. 제출 문서 목록

| 문서명 | 형식 | 필수여부 |
|---|---|---|
| 연구개발계획서 | **HWPX** | 필수 |

※ 요약서·예산서·별지는 이 양식에 없다. 공고가 요구하면 별도 확보한다.

## 2. 분량 규정 ★ 가장 자주 어기는 항목

| 항목 | 값 | 출처 |
|---|---|---|
| 전체 분량 | **10 페이지 내외** (A4 / 돋움 11pt / 줄간격 160% 기준, **표·그림 포함**) | PDF 안내문 |
| 허용 상한 | **최대 12p** | 운영 기준(2026-08-13 확정) — "내외"의 실무 해석 |
| 0. 요약문 | 1p | PDF 안내문 |
| 1. 배경·필요성 | 2p | 〃 |
| 2. 목표 | 2p | 〃 |
| 3. 내용·수행방법 | 3p | 〃 |
| 4. 기대효과·활용 | 2p | 〃 |

**분량 산정의 실측 원단위**(HWPX 빌드로 측정) — 이 값으로 착수 전에 예산을 짜면 재작업이 없다.

| 요소 | 소모 |
|---|---|
| 산문 | 약 **1,750자 / 페이지** |
| 표 | **1개당 0.4~0.6p** (행 수보다 **셀 텍스트의 줄바꿈**이 지배한다) |
| 그림 | **1장당 사실상 1p** (본문 높이의 30~35%를 차지해도 페이지 넘김이 생긴다) |

→ 10p 안에 넣으려면 **표 4~5개 + 그림 1장 + 산문 약 15,000자**가 현실적인 상한이다. 표를 10개 넘게 쓰면 산문을 아무리 줄여도 14p 아래로 내려오지 않는다.

## 3. 서식 규칙

PDF가 규정하는 것은 **A4 / 돋움 11pt / 줄간격 160%** 세 가지뿐이다. 나머지는 명시가 없으므로 도구 기본값을 쓰되 값을 문서에 남긴다.

| 항목 | 값 | 근거 |
|---|---|---|
| 용지 | **A4 세로** (`width=59528`, `height=84186` HWPUNIT) | PDF 안내문 |
| 본문 | **돋움 11pt** | PDF 안내문 |
| 줄간격 | **160%** | PDF 안내문 |
| 여백 | 좌우 8504(30mm) / 위 5668(20mm) / 아래 4252(15mm) / 머리말·꼬리말 4252 | **명시 없음** — 도구 기본값을 명시적으로 고정 |
| 표 안 글자 | 9pt | 명시 없음 — 분량 관리상 관행 |
| 장 제목 / 절 제목 | 16pt / 13pt 굵게 | 명시 없음 — 위계 확보용 |

### 조립 절차 — kordoc 프리셋 + 헤더 패치

조립은 **kordoc**(npm, MIT — `npx` 실행이라 설치가 없다)의 계획서 프리셋으로 한다.
**명령 전문과 원고 전처리는 `hwpx-writing` 스킬 §3에 있다.** 여기서는 서식 패치만 다룬다.

```bash
npx -y kordoc@^4 generate 30_proposal.md -o 30_raw.hwpx \
  --preset 계획서 --font gothic --pt 11 --line-spacing 160 --paper A4 \
  --fonts "body=돋움,heading=돋움,table=돋움" --image-dir 20_figures
```

프리셋 기본값은 **양식값과 다르다**(실측: 여백 좌우 5669=20mm, 글꼴 5종 혼용). 산출물에 다음을 패치한다.

```python
# ① 여백을 양식값으로 고정 — Contents/section*.xml
s = re.sub(r'<hp:margin[^>]*/>',
           '<hp:margin header="4252" footer="4252" gutter="0" '
           'left="8504" right="8504" top="5668" bottom="4252"/>', s)
# ② 글꼴을 "돋움"으로 통일 — Contents/header.xml
for face in ['함초롬바탕','함초롬돋움','한양신명조','한양중고딕','HY견고딕']:
    h = h.replace(f'face="{face}"', 'face="돋움"')
```

⚠️ zip 재작성 시 `mimetype` 은 **ZIP_STORED** 로 보존한다(안 하면 열리지 않는다).
목표 서식값은 동봉 `default_form_style.json`(본문 11pt / 표 9pt / 장제목 16pt 검정 / 절제목 13pt 굵게).

### ⚠️ 함정 4개 (전부 실측)

**⑴ `charPr 5`(장 제목 16pt)는 색이 `#2E74B5` 파랑이다.** Word Heading 1 색이 템플릿에 남은 것으로, 그대로 쓰면 장 제목이 파랗게 인쇄된다. 배포처 양식 본문이 실제로 쓰던 색도 `#000000` 하나뿐이었다 → **검정으로 교정**.

**⑵ 절 제목용 크기가 템플릿에 없다.** 신설하지 않으면 `1-1.` 등이 본문과 같은 11pt로 나가 위계가 사라진다 → `charPr 7`(13pt 굵게).

**⑶ "돋움"은 `돋움` 이지 `함초롬돋움` 이 아니다.** 템플릿 정의는 함초롬돋움이라 그대로 두면 안내문과 다른 글꼴이 된다.
⚠️ **글꼴 교체는 분량을 바꾼다** — 실측으로 **함초롬돋움 11p → 돋움 12p**(같은 원고, +1p)였다. 돋움이 더 넓다. **글꼴을 확정한 뒤에 분량을 맞출 것.** 반대로 하면 분량 작업을 두 번 한다.

**⑷ `charPrIDRef="0"` 이 표마다 1건씩 보이는 것은 누수가 아니다.** `<hp:tbl>` 을 **감싸는 run** 의 속성이라 인쇄되는 글자가 없고, 실제 셀은 전부 `charPr 2`(9pt)다. 표 안을 정규식으로 치환하려 들지 말 것 — 텍스트 추출 정규식이 표 첫 셀 문자열을 이 run 소속으로 잘못 묶어 보여준다.

## 4. 목차 트리와 항목별 요구 내용

양식이 지정한 장·절은 **추가·삭제·개명하지 않는다.** 원문은 `default_proposal_form.md` 참조.

| 장 | 절 | 요구 내용(PDF 원문) |
|---|---|---|
| **0. 연구 요약문** | 과제명(국문/영문) | 연구의 한 줄 요약이 드러나도록 기재 |
| | 연구 분야·기술분류 | 국가과학기술표준분류 **또는** 산업기술분류 **소분류 3순위까지(비중 %)** |
| | 전체 연구기간 | `YYYY.MM.DD ~ YYYY.MM.DD (n년 n개월)` |
| | 기술성숙도(TRL) | 착수시점(n단계) → 종료시점 목표(n단계) |
| | 최종 목표 | 연구개발과제의 최종 목표 |
| | 전체 연구내용 | 연차별·단계별 핵심 수행 내용 |
| | 연차별 목표 | 1차년도 / … / N차년도(최종) 목표 요약 |
| | 성과 활용계획 및 기대효과 | 수요처·활용내용·파급효과 요약 |
| | 핵심어(국문/영문) | **각 5개 이내** |
| **1. 연구 배경 및 필요성** | 1-1. 기술적 배경 및 국내외 동향 | 국내외 현황·전망 / 핵심 기술(제품)의 정의·범위 / **국내외 기술수준 비교, 최고 수준 보유국·기업 식별** |
| | 1-2. 시장·정책적 필요성 | 국내외 시장 규모 / 국가·산업 원천기술 확보 필요성, 정책 부합성 |
| | 1-3. 기존 기술 대비 한계 및 차별성 | 기존 접근의 문제점·한계 / 해결하려는 문제 정의 / 선행연구 대비 차별성·창의성 |
| **2. 연구 목표** | 2-1. 최종 목표 | 전체 종료시점 달성 목표. **표 또는 그림으로 요약 권장** |
| | 2-2. 단계별·연차별 목표 | **[표 A] 사용**(아래 §5) |
| | 2-3. 성과지표(KPI) 및 평가주안점 | 정성·정량 평가항목 **5개 내외, 가중치 합 100%** / 주요 성능 Spec(**단위·기준값·목표치·평가방법·평가환경** 5요소) / 평가주안점 설정근거(국내외 기술수준 기반) |
| **3. 연구 내용 및 수행 방법** | 3-1. 세부 연구 내용 | 가.1차년도 / 나.2차년도 / 다.3차년도(1단계 종료) / 라.N차년도(최종). 각 연차마다 **"무엇을(내용)·어떻게(접근 방법)·왜 그 방법인지(근거)"** 를 구체 기술 |
| | 3-2. 추진 전략 및 방법론 | 접근 방법론(알고리즘·실험설계·분석기법) / **정보 수집, 전문가 확보, 기관 간 협조 방안** / 목표달성·문제해결 대응 전략 — **3개 항목 모두** |
| | 3-3. 추진 일정 | **연차별 월 단위 간트 차트**([표 B], 아래 §5) + 주요 마일스톤·중간 산출물 명시 |
| **4. 기대효과 및 활용 방안** | 4-1. 활용 방안 | 활용 분야·구체적 활용 방안 / 기업화·기술이전·추가연구·표준화 계획 / 주요 수요처 및 확산 경로 |
| | 4-2. 기대효과 | **기술적 / 경제·산업적 / 사회적** 3개 측면 모두 |
| | 4-3. 후속 연구 및 사업화 계획 | 종료 후 후속 연구 방향 / 사업화 단계별 일정·투입 자원 / 지속가능성(수익모델·관리운영계획) |

## 5. ★ 양식이 지정한 표 2개 — 형식을 바꾸지 말 것

PDF **2~3쪽**에 실물이 있고 `default_proposal_form.md` 에 전사돼 있다. 둘 다 채워 넣어야 하는 지정 서식이다.

**[표 A] 단계별·연차별 목표** (2-2) — 열은 `구분 / 연차 / 목표` 3개, 행은 `1단계 1차년도`·`1단계 2차년도`·`…`·`N단계 N차년도(최종)`.

**[표 B] 추진 일정(간트)** (3-3) — 열은 `추진 내용` + **월 1~12 = 13열**, **1차년도만 기입**한다.
- 첫 행 예시가 `계획 수립·자료조사`에 1·2월 `■` 표기다.
- ⚠️ **분기(4Q) 단위로 4개 연차를 한 표에 넣는 것은 양식 위반**이다. 2~4차년도는 표가 아니라 서술로 덧붙인다.
- 채움 문자 `■`(U+25A0)는 kordoc 조립에서 표 셀·본문 모두 **보존**됨을 실측 확인했다(2026-08-13). 양식 원본대로 쓰면 된다.

## 6. 작성·검수 체크리스트

- [ ] 목차 0~4장, 각 3개 절을 **원문 명칭 그대로** 유지했는가
- [ ] 0장 **9개 항목**(과제명 국·영문 / 기술분류 / 기간 / TRL / 최종목표 / 전체 연구내용 / 연차별 목표 / 성과활용·기대효과 / 핵심어)이 모두 있는가
- [ ] 기술분류를 **소분류 3순위까지 비중(%) 합 100**으로 적었는가
- [ ] 핵심어가 **국문 5개 이내 / 영문 5개 이내**인가
- [ ] 2-1을 **표 또는 그림**으로 요약했는가
- [ ] 2-2가 **[표 A] 형식**인가
- [ ] KPI가 **5개 내외**이고 **가중치 합 100%**인가
- [ ] KPI 표에 **단위·기준값·목표치·평가방법·평가환경 5요소**가 다 있는가
- [ ] 평가주안점 설정근거를 **국내외 기술수준에 기반**해 썼는가
- [ ] 3-1이 연차별 **가·나·다·라** 구성이고 각 연차에 **내용·방법·근거** 3요소가 있는가
- [ ] 3-2에 **접근 방법론 / 정보수집·전문가·협조 / 대응 전략** 3개가 다 있는가
- [ ] 3-3이 **[표 B] 형식(1차년도 월 단위 12칸)**이고 **마일스톤·중간 산출물**을 명시했는가
- [ ] 4-2에 **기술적·경제산업적·사회적** 3개 측면이 다 있는가
- [ ] **분량 10p 내외(최대 12p)**, 표·그림 포함 기준으로 확인했는가 — 한컴 COM `PageCount` 로 **실측**할 것
- [ ] 서식(A4·**돋움** 11pt·160%)이 적용됐는가 — 글꼴이 함초롬돋움으로 남아 있지 않은가
- [ ] 장 제목이 **검정**인가(`#2E74B5` 파랑 잔존 0건)
- [ ] 간트 채움 문자가 `◼`인가(`■` 금지)
- [ ] 산출물 `Contents/section0.xml` 에 **XML 선언**이 있는가

## 7. 명시 없음 — 추정 금지

장평·자간, 각주 크기, 쪽번호 위치·형식, 표·그림 캡션 번호 규칙, 파일명 규칙, 제출 부수.
공고에 별도 지시가 있으면 그것을 따르고, 없으면 사용자 확인 후 결정한다.
